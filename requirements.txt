# 投资估值系统 - Python依赖包
# Python 3.8+ required

# ============================================================================
# 核心依赖 - 必需
# ============================================================================

# 数据处理和分析
numpy>=1.24.0              # 数值计算
pandas>=2.0.0             # 数据结构和分析

# 可视化
matplotlib>=3.7.0          # 图表绘制（可选，用于生成图表）

# 统计分析
scipy>=1.10.0             # 统计分析（蒙特卡洛模拟等）

# 数据源
tushare>=1.2.60           # A股数据获取（需要申请Token）

# Web框架
fastapi>=0.100.0          # API框架
uvicorn[standard]>=0.23.0 # ASGI服务器
pydantic>=2.0.0           # 数据验证
python-multipart>=0.0.6   # 表单数据支持

# 工具库
python-dateutil>=2.8.0    # 日期处理

# ============================================================================
# 可选依赖 - 根据需要安装
# ============================================================================

# 开发工具
# pytest>=7.0.0            # 单元测试
# pytest-cov>=3.0.0       # 测试覆盖率
# black>=22.0.0           # 代码格式化
# flake8>=4.0.0           # 代码检查
# mypy>=1.0.0             # 类型检查

# 数据分析增强
# jupyter>=1.0.0          # Jupyter notebook支持
# seaborn>=0.12.0         # 统计可视化

# 性能优化
# numba>=0.56.0           # JIT编译加速（可选）

# ============================================================================
# 安装说明
# ============================================================================
#
# 基础安装：
#   pip install -r requirements.txt
#
# 开发环境安装（包含所有可选）：
#   pip install -r requirements.txt
#   pip install pytest pytest-cov black flake8 mypy jupyter
#
# 使用虚拟环境（推荐）：
#   python -m venv venv
#   source venv/bin/activate  # Linux/Mac
#   venv\Scripts\activate     # Windows
#   pip install -r requirements.txt
#
# ============================================================================

# 数据库ORM（新增，用于历史记录）
sqlalchemy>=2.0.0          # 数据库ORM

